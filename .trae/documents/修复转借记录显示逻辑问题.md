## 修复计划

### 问题1：PC端my\_records.html搜索功能中转借视角区分缺失

* 修改JavaScript的`renderSearchResults`函数，根据当前用户是转出方还是转入方显示"转借"或"被转借"

* 添加`getTransferDisplayType`函数处理转借记录显示

### 问题2：统一PC端my\_records.html和device\_detail.html的转借显示逻辑

* 在device\_detail.html中添加对'被转借：'前缀的处理

* 确保两个页面的转借判断逻辑一致

### 问题3：移动端my\_records.html转借显示逻辑

* 移动端当前统一显示为"被转借"，需要根据用户角色区分显示"转借"或"被转借"

* 添加与PC端相同的视角判断逻辑

### 修改文件

1. `user_service/templates/pc/my_records.html` - 修复搜索功能转借显示
2. `user_service/templates/pc/device_detail.html` - 统一转借逻辑
3. `user_service/templates/my_records.html` - 修复移动端转借显示

